name: aondepetflutter
description: "Prototipo aondepet em flutter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/img.png  # Certifique-se de que o caminho está correto
    # Ou, se quiser incluir todos os arquivos na pasta assets:
    # - assets/
